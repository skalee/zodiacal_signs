require 'spec_helper'

describe ZodiacalSigns do
  it 'has a version number' do
    expect(ZodiacalSigns::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
